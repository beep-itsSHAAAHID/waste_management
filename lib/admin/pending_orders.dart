import 'package:flutter/material.dart';

class PendingOrdersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pending Orders'),
      ),
      body: Center(
        child: Text('Pending Orders Page'),
      ),
    );
  }
}
