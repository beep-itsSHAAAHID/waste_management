import 'package:flutter/material.dart';

class TotalOrdersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Total Orders'),
      ),
      body: Center(
        child: Text('Total Orders Page'),
      ),
    );
  }
}
