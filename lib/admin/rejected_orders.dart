import 'package:flutter/material.dart';

class RejectedOrdersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Rejected Orders'),
      ),
      body: Center(
        child: Text('Rejected Orders Page'),
      ),
    );
  }
}
